#include <iostream>
#include "shape.h"
#include "ShapeContainer.h"
int main(int argc, char* argv[])
{
    std::string fileName;
    if(argc > 1)
    {
        fileName = argv[1];
        Shape run;
        run.
    }
    Shape Run;
    
}